# 如何为 TodoX 创建托盘图标

## 问题说明
打包后托盘图标不显示是因为 `assets/tray-icon.png` 文件不存在。

## 解决方案

### 方案1：使用在线工具创建图标（推荐）

1. **访问图标生成网站**：
   - https://www.favicon-generator.org/
   - https://icon-icons.com/
   - https://www.flaticon.com/

2. **下载或创建简单图标**：
   - 搜索 "checklist icon" 或 "todo icon"
   - 选择一个简单的图标
   - 下载 PNG 格式

3. **调整尺寸**：
   - 使用在线工具调整为 16x16 或 32x32 像素
   - https://www.iloveimg.com/resize-image
   - 或使用 Windows 画图工具调整

4. **保存文件**：
   - 命名为 `tray-icon.png`
   - 保存到 `assets/` 目录

### 方案2：使用 Windows 画图创建简单图标

1. 打开 Windows 画图程序
2. 创建 32x32 像素的新图像
3. 绘制一个简单的图标（如勾选框）
4. 保存为 `tray-icon.png` 到 assets 目录

### 方案3：使用命令行创建简单图标

在项目根目录运行以下命令（需要安装 ImageMagick）：

```bash
# 创建一个简单的蓝色方块图标
magick -size 32x32 xc:#667eea -fill white -pointsize 20 -gravity center -annotate +0+0 "✓" assets/tray-icon.png
```

### 方案4：暂时使用 Electron 默认图标

如果暂时不想创建图标，代码已经处理了图标不存在的情况，会使用 Electron 的空图标。但建议创建一个图标以获得更好的用户体验。

## 推荐的图标设计

- **尺寸**：16x16 或 32x32 像素（推荐 32x32）
- **格式**：PNG（支持透明背景）
- **颜色**：简单的单色或双色设计
- **内容**：勾选框、列表图标或应用首字母 "T"

## 图标示例

### 简单勾选框图标
```
□ ← 空心方框
✓ ← 勾选标记
📋 ← 列表图标
```

### 颜色建议
- 主题色：#667eea（紫色，与应用主题一致）
- 背景：透明或白色
- 图标：白色或黑色（取决于系统托盘背景）

## 验证图标

创建图标后，运行应用检查：

```bash
npm start
```

在系统托盘（任务栏右下角）查看图标是否正确显示。

## 打包后验证

打包应用后，图标应该会正确显示在系统托盘中：

```bash
npm run build:win
```

安装并运行打包后的应用，检查托盘图标。

