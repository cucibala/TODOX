# 桌面背景模式使用指南

## 🖥️ 什么是桌面背景模式？

桌面背景模式是 TodoX 的一个创新功能，让任务清单像桌面小部件一样显示在桌面上。

### 核心特点
- 📍 **桌面层级**：显示在所有窗口之下，但在桌面壁纸之上
- 🎨 **半透明效果**：毛玻璃背景，美观且不遮挡壁纸
- 👁️ **随时可见**：按 Win+D 回到桌面即可看到任务清单
- 📝 **直接操作**：无需切换窗口，直接在桌面上管理任务

## 🚀 如何使用

### 方法1：通过标题栏按钮
1. 点击标题栏最左边的"显示器"图标（🖥️）
2. 窗口立即变成半透明并固定在桌面上
3. 再次点击可退出桌面模式

### 方法2：通过系统托盘
1. 右键点击系统托盘中的 TodoX 图标
2. 选择"桌面背景模式"
3. 或点击"退出桌面模式"返回普通模式

## 💡 典型使用场景

### 场景1：工作监控
```
用途：在工作时随时查看待办事项
操作：
1. 进入桌面模式
2. 将窗口放在桌面右下角或左下角
3. 工作时按 Win+D 快速查看任务
4. 完成后按 Win+D 继续工作
```

### 场景2：专注学习
```
用途：学习时提醒自己的学习计划
操作：
1. 创建今日学习任务清单
2. 进入桌面模式并置于桌面一角
3. 休息时看桌面，检查学习进度
4. 完成任务时直接在桌面上打勾
```

### 场景3：每日计划
```
用途：将每天的计划固定在桌面上
操作：
1. 早晨规划今日任务
2. 进入桌面模式
3. 全天工作时，任务清单始终在桌面背景
4. 随时可以完成或添加任务
```

## 🎨 视觉效果

桌面模式下的视觉特点：
- **背景**：半透明毛玻璃效果（透明度 85%）
- **模糊**：10px 背景模糊，营造层次感
- **阴影**：柔和阴影，与桌面区分
- **边框**：白色半透明边框
- **任务卡片**：80% 透明度，悬停时 90%

## ⚙️ 技术细节

### Windows 平台
- 使用 `transparent: true` 启用窗口透明
- 设置窗口层级为 `normal, -1` (最底层)
- 不显示在任务栏（`skipTaskbar: true`）
- 窗口置顶功能自动禁用

### macOS 平台
- 透明窗口 + Dock 隐藏
- 设置为桌面层级窗口
- 保持半透明效果

### Linux 平台
- 透明窗口支持
- 桌面层级设置
- 可能需要 compositor 支持

## 🔧 注意事项

### 1. 与置顶功能互斥
- 进入桌面模式后，窗口置顶功能会自动禁用
- 这是因为桌面模式需要窗口在最底层
- 退出桌面模式后，置顶功能恢复可用

### 2. 性能影响
- 透明和毛玻璃效果需要一定的 GPU 资源
- 在低配置电脑上可能略微影响性能
- 如感觉卡顿，可以退出桌面模式

### 3. 多显示器
- 每个显示器都可以单独设置
- 窗口会记住在哪个显示器上
- 拖动到其他显示器后位置会保持

### 4. 窗口大小
- 桌面模式支持任意窗口大小
- 建议使用迷你模式（350px）更节省空间
- 也可以使用完整模式查看所有功能

## 📊 对比：三种显示模式

| 模式 | 层级 | 透明 | 使用场景 |
|------|------|------|----------|
| **普通模式** | 正常 | 不透明 | 日常使用，完整功能 |
| **置顶模式** | 最顶层 | 不透明 | 工作时保持可见 |
| **桌面模式** | 最底层 | 半透明 | 桌面背景查看 |

## 🎯 最佳实践

### 推荐组合1：桌面+迷你
```
设置：桌面模式 + 迷你模式
位置：右下角
优点：占用空间小，始终可见，美观
```

### 推荐组合2：桌面+完整
```
设置：桌面模式 + 完整模式
位置：居中或侧边
优点：功能完整，适合大屏幕
```

### 推荐组合3：智能切换
```
早晨：完整模式，规划任务
白天：桌面模式，监控进度
晚上：置顶模式，完成总结
```

## 💾 数据安全

- 桌面模式下所有数据操作正常
- 添加、删除、编辑任务都会立即保存
- 切换模式时不会丢失任何数据
- 图片附件功能正常工作

## ❓ 常见问题

**Q: 为什么看不到桌面上的任务清单？**
A: 桌面模式是显示在所有窗口之下的，需要按 Win+D 显示桌面或最小化所有窗口。

**Q: 如何退出桌面模式？**
A: 点击标题栏的桌面图标，或右键托盘图标选择"退出桌面模式"。

**Q: 桌面模式下能添加任务吗？**
A: 完全可以！所有功能都正常工作，包括添加、编辑、删除任务。

**Q: 能同时开启置顶和桌面模式吗？**
A: 不能，这两个模式互斥。桌面模式需要窗口在最底层，而置顶模式需要在最顶层。

**Q: 桌面模式会影响性能吗？**
A: 有轻微影响，因为使用了透明和模糊效果。如果电脑性能较弱，可以不使用此功能。

**Q: 重启应用后会记住桌面模式吗？**
A: 会的！设置会自动保存，下次启动时恢复上次的模式。

## 🎓 进阶技巧

### 技巧1：配合虚拟桌面
- Windows 10/11 支持虚拟桌面
- 可以在特定虚拟桌面使用桌面模式
- 切换桌面时清单始终可见

### 技巧2：配合壁纸
- 选择较暗的壁纸，清单更清晰
- 或选择留白较多的壁纸，放置在空白区域
- 纯色壁纸效果最佳

### 技巧3：配合快捷键
- Win+D：显示桌面（查看清单）
- Win+M：最小化所有窗口
- Alt+Tab：切换窗口时跳过桌面模式窗口

## 🔮 未来规划

可能的改进方向：
- [ ] 自定义透明度
- [ ] 自定义模糊程度
- [ ] 快捷键切换桌面模式
- [ ] 桌面模式专用主题
- [ ] 点击穿透选项（只查看不操作）

