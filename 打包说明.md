TodoX 打包说明
================

## 快速打包（Windows 用户）

**⚠️ 打包前必做：创建托盘图标！**

### 第0步：创建托盘图标（必需）
在打包前，必须先创建托盘图标，否则打包后托盘图标不显示：

1. 准备一个 32x32 像素的 PNG 图标
2. 命名为 `tray-icon.png`
3. 放在 `assets/` 目录
4. 或参考 `创建托盘图标说明.md`

### 第1步：安装依赖（只需执行一次）
```bash
npm install
```

### 第2步：打包应用（在 Windows 上运行）
```bash
npm run build:win
```

### 第3步：查看打包结果
等待打包完成，安装包位于：
```
dist\TodoX Setup 2.1.0.exe
```

### 第4步：安装使用
双击安装包即可安装使用


## 其他命令

- 打包 macOS 版本：npm run build:mac
- 打包 Linux 版本：npm run build:linux
- 自动打包（根据当前系统）：npm run build


## 注意事项

1. **首次打包需要下载约 100MB 的 Electron 运行时**，请耐心等待
2. **打包后的安装包约 150-200MB**（包含完整的浏览器引擎）
3. **托盘图标是必需的**：
   - ✅ 打包前必须创建 `assets/tray-icon.png`
   - ✅ 推荐尺寸：32x32 像素
   - ✅ 格式：PNG（支持透明背景）
   - ❌ 如果不创建，打包后托盘图标不显示
4. 如需自定义应用图标（可选）：
   - 创建 assets 目录
   - 添加 icon.ico 文件（256x256 或更大）


## 常见问题

Q: 打包失败怎么办？
A: 检查是否已安装 Node.js 和 npm，尝试删除 node_modules 后重新 npm install

Q: 打包很慢？
A: 首次打包需要下载依赖，后续打包会快很多

Q: 能否在 Windows 上打包 Mac 版本？
A: 技术上可行但不推荐，建议在对应平台打包


详细说明请查看 build-guide.md

