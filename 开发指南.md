# TodoX 开发指南

## 🚀 快速开始

### 1. 安装依赖

```bash
# 安装 Electron 依赖（根目录）
npm install

# 安装 Vue 依赖（src 目录）
cd src
npm install
cd ..
```

### 2. 开发模式

**两个终端同时运行：**

终端 1 - 启动 Vite 开发服务器：
```bash
cd src
npm run dev
```

终端 2 - 启动 Electron（开发模式）：
```bash
npm run dev
```

> **注意**：必须先启动 Vite 开发服务器（终端1），然后再启动 Electron（终端2），否则 Electron 会加载失败。

### 3. 生产模式运行

```bash
# 自动构建 Vue 并运行 Electron
npm start
```

### 4. 打包发布

```bash
npm run build        # 全平台
npm run build:win    # Windows
npm run build:mac    # macOS  
npm run build:linux  # Linux
```

## 📁 项目结构

```
TodoX/
├── main.js              # Electron 主进程
├── preload.js           # Electron 预加载脚本
├── package.json         # Electron 项目配置
│
└── src/                 # Vue 项目
    ├── package.json     # Vue 依赖
    ├── vite.config.js   # Vite 配置
    ├── index.html       # HTML 入口
    └── src/
        ├── main.js      # Vue 应用入口
        ├── App.vue      # 根组件
        ├── assets/      # 静态资源
        ├── components/  # Vue 组件
        ├── stores/      # Pinia 状态管理
        └── utils/       # 工具函数
```

## 🔧 工作原理

### 开发模式 (`npm run dev`)
- Electron 加载 Vite 开发服务器 URL: `http://localhost:5173`
- 支持热模块替换 (HMR)
- 自动打开开发者工具
- 实时预览代码修改

### 生产模式 (`npm start`)
- 先构建 Vue 应用到 `dist-vue/` 目录
- Electron 加载构建好的静态文件
- 性能优化的生产版本

## 🛠️ 开发流程

### 添加新功能

1. **创建 Vue 组件**
```bash
# 在 src/src/components/ 目录下创建
touch src/src/components/NewComponent.vue
```

2. **使用组件**
```vue
<!-- 在需要的地方导入 -->
<script setup>
import NewComponent from './components/NewComponent.vue'
</script>

<template>
  <NewComponent />
</template>
```

3. **添加状态管理**
```javascript
// 在对应的 store 文件中添加
// src/src/stores/app.js
// src/src/stores/project.js
// src/src/stores/todo.js
```

### 调试技巧

#### 1. 浏览器调试（推荐）
```bash
cd src
npm run dev
# 在浏览器中打开 http://localhost:5173
# 使用 Chrome/Edge DevTools 调试
```

#### 2. Electron 调试
```bash
npm run dev
# Electron 窗口会自动打开开发者工具
# 或按 F12 打开
```

#### 3. Vue DevTools
- 安装 Vue DevTools 浏览器扩展
- 可以查看组件树和 Pinia store 状态

## 📝 常用命令

```bash
# 开发
npm run dev          # 启动 Electron 开发模式
cd src && npm run dev  # 启动 Vite 开发服务器

# 构建
npm run vue:build    # 仅构建 Vue 应用
npm start           # 构建并运行 Electron

# 打包
npm run build:win    # 打包 Windows 版本
npm run build:mac    # 打包 macOS 版本
npm run build:linux  # 打包 Linux 版本

# 清理
rm -rf dist-vue     # 清除 Vue 构建产物
rm -rf dist         # 清除 Electron 打包产物
```

## ⚠️ 常见问题

### Q1: Electron 启动时显示空白页面？
**A:** 确保先启动 Vite 开发服务器：
```bash
# 终端 1
cd src
npm run dev

# 等待 Vite 启动后，在终端 2
npm run dev
```

### Q2: 修改代码后没有生效？
**A:** 
- 开发模式：检查 Vite 是否正在运行，应该会自动热更新
- 生产模式：需要重新运行 `npm start`

### Q3: 打包时出错？
**A:** 
1. 确保先构建 Vue: `npm run vue:build`
2. 检查 `dist-vue/` 目录是否存在
3. 删除 `node_modules` 重新安装依赖

### Q4: 无法访问 electronAPI？
**A:** 
- 在浏览器开发时，`window.electronAPI` 不可用
- 只在 Electron 环境中可用
- 可以添加 mock 数据用于浏览器调试

### Q5: Vite 端口被占用？
**A:** 修改 `src/vite.config.js` 中的端口：
```javascript
export default defineConfig({
  server: {
    port: 5174  // 改成其他端口
  }
})
```
然后在 `main.js` 中也要修改对应的 URL。

## 🎯 最佳实践

### 1. 开发建议
- 优先在浏览器中开发和调试 UI
- 涉及 Electron API 的功能再在 Electron 中测试
- 使用 Vue DevTools 调试状态

### 2. 代码规范
- 组件命名使用大驼峰：`MyComponent.vue`
- 文件命名使用小驼峰：`utils.js`
- Store 命名使用小写：`app.js`

### 3. 性能优化
- 图片使用懒加载
- 大列表使用虚拟滚动
- 避免在 computed 中执行复杂计算

### 4. 提交前检查
```bash
# 1. 确保能构建成功
npm run vue:build

# 2. 确保能正常运行
npm start

# 3. 确保能正常打包
npm run build:win
```

## 📚 相关文档

- [Vue 3 文档](https://cn.vuejs.org/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)
- [Vite 文档](https://cn.vitejs.dev/)
- [Electron 文档](https://www.electronjs.org/zh/docs/latest/)

## 🎨 自定义主题

修改 `src/src/assets/styles/variables.css`：

```css
:root {
  --primary-color: #667eea;  /* 主题色 */
  --primary-dark: #5568d3;   /* 深色主题 */
  --primary-light: #7c8ff0;  /* 浅色主题 */
  /* ... 其他颜色变量 */
}
```

## 🔄 数据迁移

数据格式与之前完全兼容，无需迁移。

数据存储位置：
- Windows: `%APPDATA%/todox/`
- macOS: `~/Library/Application Support/todox/`
- Linux: `~/.config/todox/`

## 📊 性能监控

在开发模式下可以查看性能：

```javascript
// 在 Vue 组件中
import { onMounted } from 'vue'

onMounted(() => {
  console.time('component-mount')
  // 你的代码
  console.timeEnd('component-mount')
})
```

## 🚀 下一步

- [ ] 添加 TypeScript 支持
- [ ] 添加单元测试
- [ ] 添加 E2E 测试
- [ ] 优化打包体积
- [ ] 添加自动更新功能

