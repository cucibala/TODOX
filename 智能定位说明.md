# TodoX 智能定位功能说明

## 🤖 智能定位是什么？

智能定位是 TodoX 的一个智能窗口管理功能，当你将窗口拖动到屏幕右上角区域时，应用会自动：
1. 切换到迷你模式（350x500像素）
2. 启用窗口置顶（始终在最前面）

这样你就可以轻松地将任务清单固定在桌面角落，随时查看待办事项。

## 📍 如何触发智能定位？

### 触发区域
- **右侧30%区域**：屏幕右侧30%的宽度范围
- **顶部30%区域**：屏幕顶部30%的高度范围
- **右上角区域**：同时满足上述两个条件的区域

### 操作步骤
1. 使用鼠标拖动标题栏
2. 将窗口移动到屏幕右上角
3. 放开鼠标
4. 等待约300毫秒（防抖处理）
5. 自动切换到迷你模式并置顶
6. 看到提示"已切换到迷你模式"

## 🎯 使用场景示例

### 场景1：工作时查看待办事项
```
┌─────────────────────────────────────┐
│                                     │
│                    ┌──────┐         │
│                    │TodoX │         │  ← 拖到这里
│                    │      │         │
│                    │ □ 任务1         │
│                    │ □ 任务2         │
│                    │ ✓ 任务3         │
│                    └──────┘         │
│                                     │
│  主工作区域                          │
│  （浏览器、编辑器等）                 │
│                                     │
└─────────────────────────────────────┘
```

### 场景2：多显示器使用
- 在副显示器右上角放置任务清单
- 主显示器全屏工作
- 随时瞟一眼任务进度

## ⚙️ 技术细节

### 检测逻辑
```javascript
// 计算窗口中心点
windowCenterX = windowX + windowWidth / 2
windowCenterY = windowY + windowHeight / 2

// 判断是否在右上角
isInTopRight = 
  windowCenterX >= screenWidth * 0.7 &&
  windowCenterY <= screenHeight * 0.3
```

### 防抖处理
- 窗口移动事件会频繁触发
- 使用300毫秒防抖，避免误触发
- 只在停止移动后才检测位置

### 自动操作
1. 检测到窗口在右上角
2. 且当前不是迷你模式时
3. 自动切换到迷你模式
4. 自动启用置顶
5. 保存设置到本地

## 💡 提示和技巧

### 如果想关闭智能定位
目前智能定位是自动启用的，但你可以：
- 手动切换回完整模式（点击分屏按钮）
- 将窗口移动到其他位置后手动操作

### 如果智能定位没有触发
检查以下几点：
1. 窗口是否在右上角区域（右侧30%且顶部30%）
2. 窗口是否已经是迷你模式（只在非迷你模式时触发）
3. 等待约300毫秒，不要快速移动

### 最佳实践
1. **第一次使用**：先切换到完整模式，然后拖到右上角体验自动切换
2. **多显示器**：在每个显示器都可以独立触发
3. **固定位置**：找到合适的位置后，应用会记住窗口位置

## 🎨 视觉反馈

当智能定位触发时，你会看到：
- 窗口大小从1000x700缩小到350x500
- 侧边栏自动隐藏
- 布局自动调整为紧凑模式
- 屏幕底部显示"已切换到迷你模式"提示
- 图钉按钮变为高亮状态（表示已置顶）

## 🔧 高级设置

### 修改触发区域
如果你想修改触发区域的大小，可以编辑 `main.js` 文件：

```javascript
// 找到这些行并修改百分比
const rightThreshold = workArea.x + workArea.width * 0.7;  // 0.7 = 右侧30%
const topThreshold = workArea.y + workArea.height * 0.3;   // 0.3 = 顶部30%
```

### 修改防抖时间
```javascript
// 在 main.js 中找到这行
moveTimeout = setTimeout(() => {
  checkWindowPosition();
}, 300);  // 300毫秒，可以调整
```

## ❓ 常见问题

**Q: 为什么只有右上角才能触发？**
A: 右上角通常是桌面空间较多的地方，适合放置小工具。左上角通常有开始菜单等系统元素。

**Q: 可以在左上角也触发吗？**
A: 可以通过修改代码实现，但默认只支持右上角。

**Q: 智能定位会影响性能吗？**
A: 不会。使用了防抖机制，只在停止移动后才检测，对性能影响微乎其微。

**Q: 可以禁用智能定位吗？**
A: 可以在代码中注释掉窗口移动监听事件，但建议保留这个功能。

## 🎓 学习资源

- [Electron Screen API](https://www.electronjs.org/docs/latest/api/screen)
- [BrowserWindow Events](https://www.electronjs.org/docs/latest/api/browser-window#instance-events)
- [工作区域计算](https://www.electronjs.org/docs/latest/api/structures/display#displayworkarea)

