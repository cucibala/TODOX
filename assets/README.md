# Assets 目录

这个目录用于存放应用资源文件。

## 托盘图标

可以在这里放置 `tray-icon.png` 作为系统托盘图标。

**推荐尺寸**: 16x16 或 32x32 像素（PNG格式）

如果没有图标文件，应用会使用默认的空图标。

## 如何添加托盘图标

1. 准备一个 16x16 或 32x32 像素的 PNG 图片
2. 命名为 `tray-icon.png`
3. 放置在此目录中
4. 重新启动应用即可生效

