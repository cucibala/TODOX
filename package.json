{
  "name": "todox",
  "version": "3.1.0",
  "description": "一个现代化的桌面任务清单应用 - 支持项目管理、进度追踪、多图片附件、桌面背景模式、窗口置顶、迷你模式、系统托盘",
  "main": "main.js",
  "scripts": {
    "start": "npm run vue:build && electron .",
    "dev": "electron . --dev",
    "vue:dev": "cd src && npm run dev",
    "vue:build": "cd src && npm run build",
    "build": "npm run vue:build && electron-builder",
    "build:win": "npm run vue:build && electron-builder --win",
    "build:mac": "npm run vue:build && electron-builder --mac",
    "build:linux": "npm run vue:build && electron-builder --linux"
  },
  "keywords": ["electron", "todo", "task", "productivity"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.6.4"
  },
  "build": {
    "appId": "com.todox.app",
    "productName": "TodoX",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "dist-vue/**/*",
      "package.json",
      "assets/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ]
    },
    "mac": {
      "target": "dmg",
      "category": "public.app-category.productivity"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Utility"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}

