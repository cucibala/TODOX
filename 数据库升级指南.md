# 数据库升级指南

## 概述

TodoX 使用 SQLite 数据库，内置版本管理系统，支持平滑升级数据库架构。

## 当前版本

- **数据库版本**: v2
- **应用版本**: v3.9.0

## 版本管理机制

### 自动检测与迁移

应用启动时会自动：
1. 检查数据库当前版本
2. 对比目标版本
3. 逐版本执行迁移脚本
4. 记录版本历史

### 版本表结构

```sql
CREATE TABLE db_version (
  version INTEGER PRIMARY KEY,
  applied_at TEXT NOT NULL,
  description TEXT
);
```

## 如何添加新的数据库迁移

### 步骤 1: 更新版本号

在 `database.js` 顶部修改版本号：

```javascript
// 当前数据库版本
const DATABASE_VERSION = 2;  // 从 1 改为 2
```

### 步骤 2: 编写迁移函数

在 `getMigrationFunction()` 方法中添加新版本的迁移逻辑：

```javascript
getMigrationFunction(version) {
  const migrations = {
    // 版本 2 - 添加标签功能
    2: function() {
      // 创建标签表
      this.db.exec(`
        CREATE TABLE IF NOT EXISTS tags (
          id TEXT PRIMARY KEY,
          name TEXT NOT NULL,
          color TEXT,
          created_at TEXT NOT NULL
        );
      `);
      console.log('  - 创建 tags 表');
      
      // 为任务表添加标签字段
      this.db.exec(`
        ALTER TABLE todos ADD COLUMN tags TEXT;
      `);
      console.log('  - 为 todos 表添加 tags 字段');
    },
    
    // 版本 3 - 添加其他功能
    3: function() {
      // 你的迁移代码...
    },
  };
  
  return migrations[version];
}
```

### 步骤 3: 测试迁移

1. 备份现有数据库
2. 重启应用，观察控制台输出
3. 验证数据完整性
4. 测试新功能

## 迁移示例

### 示例 1: 添加新字段

```javascript
2: function() {
  this.db.exec(`
    ALTER TABLE todos ADD COLUMN priority_level INTEGER DEFAULT 1;
  `);
  console.log('  - 为 todos 表添加 priority_level 字段');
}
```

### 示例 2: 创建新表

```javascript
2: function() {
  this.db.exec(`
    CREATE TABLE IF NOT EXISTS categories (
      id TEXT PRIMARY KEY,
      name TEXT NOT NULL,
      icon TEXT,
      created_at TEXT NOT NULL
    );
  `);
  console.log('  - 创建 categories 表');
  
  // 创建索引
  this.db.exec(`
    CREATE INDEX IF NOT EXISTS idx_categories_name ON categories(name);
  `);
  console.log('  - 创建 categories 索引');
}
```

### 示例 3: 数据转换

```javascript
3: function() {
  // 1. 添加新字段
  this.db.exec(`
    ALTER TABLE todos ADD COLUMN status TEXT DEFAULT 'pending';
  `);
  
  // 2. 迁移现有数据
  this.db.exec(`
    UPDATE todos SET status = 'completed' WHERE completed = 1;
  `);
  
  this.db.exec(`
    UPDATE todos SET status = 'pending' WHERE completed = 0;
  `);
  
  console.log('  - 迁移 completed 字段到 status 字段');
}
```

### 示例 4: 重命名字段（SQLite 限制）

SQLite 不支持直接重命名字段，需要重建表：

```javascript
4: function() {
  // 1. 创建新表
  this.db.exec(`
    CREATE TABLE todos_new (
      id TEXT PRIMARY KEY,
      project_id TEXT,
      title TEXT NOT NULL,  -- 原 text 字段改名为 title
      completed INTEGER DEFAULT 0,
      priority TEXT DEFAULT 'medium',
      due_date TEXT,
      created_at TEXT NOT NULL,
      completed_at TEXT,
      "order" INTEGER DEFAULT 0,
      updated_at TEXT NOT NULL,
      FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
    );
  `);
  
  // 2. 复制数据
  this.db.exec(`
    INSERT INTO todos_new 
    SELECT id, project_id, text as title, completed, priority, due_date, 
           created_at, completed_at, "order", updated_at 
    FROM todos;
  `);
  
  // 3. 删除旧表
  this.db.exec(`DROP TABLE todos;`);
  
  // 4. 重命名新表
  this.db.exec(`ALTER TABLE todos_new RENAME TO todos;`);
  
  // 5. 重建索引
  this.db.exec(`
    CREATE INDEX IF NOT EXISTS idx_todos_project ON todos(project_id);
  `);
  
  console.log('  - 将 text 字段重命名为 title');
}
```

## 版本查询

### 查看当前版本

```javascript
const currentVersion = db.getDatabaseVersion();
console.log('当前数据库版本:', currentVersion);
```

### 查看版本历史

```javascript
const history = db.getVersionHistory();
console.log('版本历史:');
history.forEach(v => {
  console.log(`v${v.version} - ${v.description} (${v.applied_at})`);
});
```

## 最佳实践

### 1. 版本号递增

- 每次架构变更必须递增版本号
- 不要跳版本号（如从 1 直接到 3）
- 版本号只增不减

### 2. 向后兼容

- 尽量添加字段而非删除
- 添加字段时设置默认值
- 保留旧字段一段时间再删除

### 3. 测试充分

- 在测试数据库上先验证
- 准备回滚方案
- 测试所有数据访问路径

### 4. 事务保护

- 迁移函数会自动包装在事务中
- 确保迁移的原子性
- 失败会自动回滚

### 5. 日志详细

- 每个迁移步骤添加日志
- 便于追踪问题
- 帮助用户了解升级过程

### 6. 数据备份

- 升级前自动备份数据库
- 保留迁移前的数据
- 便于问题排查和恢复

## 故障处理

### 迁移失败

如果迁移失败：
1. 检查控制台错误日志
2. 恢复备份数据库
3. 修复迁移脚本
4. 重新测试

### 版本不一致

如果版本号混乱：
1. 手动检查 `db_version` 表
2. 必要时清空版本表
3. 重新初始化数据库

### 数据丢失

1. 从 `json-backup/` 恢复旧数据
2. 从数据库备份恢复
3. 联系技术支持

## 控制台输出示例

### 新数据库初始化

```
数据库初始化成功: F:\...\todox.db
新数据库已初始化为版本 v1
数据库版本已更新至 v1: 初始数据库版本
```

### 版本升级

```
数据库初始化成功: F:\...\todox.db
开始数据库迁移：v1 → v3
正在执行迁移到版本 v2...
  - 创建 tags 表
  - 为 todos 表添加 tags 字段
✓ 成功迁移到版本 v2
正在执行迁移到版本 v3...
  - 迁移 completed 字段到 status 字段
✓ 成功迁移到版本 v3
数据库迁移完成，当前版本 v3
```

## 版本历史

### v2 (2025-10-21)

**子任务功能增强**

- 为 subtasks 表添加 `weight` 字段（权重，用于计算进度）
- 为 subtasks 表添加 `requires_input` 字段（是否必填输入）
- 为 subtasks 表添加 `input_value` 字段（输入值）

### v1 (2025-10-21)

**初始版本**

- 项目表 (projects)
- 任务表 (todos)
- 子任务表 (subtasks)
- 进度记录表 (progress_records)
- 图片附件表 (images)
- 会话表 (conversations)
- 消息表 (messages)
- 设置表 (settings)
- 版本表 (db_version)

---

## 需要帮助？

如果遇到问题：
1. 查看控制台错误日志
2. 检查 `数据库升级指南.md`
3. 参考示例代码
4. 提交 Issue 到 GitHub

